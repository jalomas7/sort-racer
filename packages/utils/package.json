{
    "name": "@packages/utils",
    "private": true,
    "version": "1.0.0",
    "main": "./dist/index.js",
    "typings": "./dist/index.d.ts",
    "scripts": {
        "build": "npm run clean && npm run compile",
        "clean": "rm -rf ./dist",
        "compile": "tsc -p tsconfig.json"
    },
    "devDependencies": {
        "typescript": "^3.9.5"
    }
}
